# Evolver / EvoMap 记忆系统分析

**目标**：理解 EvoMap `evolver` 的记忆与进化相关设计，为 BlueCortexCE（旁路记忆）提炼可翻译、可落地的思想（非照搬 GEP 运行时）。

**数据来源**：本地 `EvoMap/evolver/` 源码；本仓库见 `docs/ARCHITECTURE-zh-CN.md` 等。

**cron 巡检 2026-05-07 03:45**：目录 **134** 个 .md；最大 45633B；所有 < 50KB ✅；**backlog 全项已勾选 ✅**；backlog **0 项未决**；上游 v1.79.1 已覆盖（本地 checkout v1.47.0 `e72778e`，main 分支领先 56 commits）；无新增可分析源码（v1.78.10→v1.79.1 仅新增 `scripts/build_binaries.js` 已覆盖 + 3个重度混淆模块 explore/shield/hubVerify/integrityCheck 不可分析）；proxy 子系统（sync/engine+inbound+outbound / extensions/dmHandler+sessionHandler+skillUpdater / task/monitor）已通过 doc 98 部分覆盖；**持续维护 pass，无新增分析任务。** / gitOps 三种回滚策略hard/stash/none + 关键文件保护名单（MEMORY.md/SOUL.md等）+ 路径遍历双重防护normAbs.startsWith(normRepo) + DIFF_SNAPSHOT 8000截断 + stash降级hard容错 / hubReview 非阻塞评审提交（outcome×constraint双因素4档评分 + hub_review_history.json防重复500条 + 10s超时AbortController）/ CE P1关键文件保护+P1路径遍历防护+P3复用资产评审；详见 [`121`](./121-gitOps-hubReview-deep-dive.md)）。新增 **`122`** `paths.js` 路径架构+Session Scope隔离深度（133L纯JS / 集中路径管理+多租户Scope隔离EVOLVER_SESSION_SCOPE+三层路径遍历防护白名单+长度128+禁止点点 + 自身目录优先防止误用父仓库 / getWorkspaceRoot三层fallback（OPENCLAW_WORKSPACE优先→workspace子目录→repoRoot）+ 与doc 80 config集中化互补构成配置中枢 / CE P1多租户路径隔离+P1路径遍历防护+P2集中PathConfig；详见 [`122`](./122-paths-js-path-architecture-deep-dive.md)）。此前 **`120`** v1.79.0/v1.79.1 Delta（+89L daemon 重写+Cycle Hard-Timeout Promise.race 45min+Windows Respawn Fix+dotenv加载顺序修复+201 genes+4 capsules；详见 [`120`](./120-v1790-v1791-cycle-hard-timeout-and-windows-respawn-deep-dive.md)）。

**cron 巡检 2026-05-07 01:34**：目录 **133** 个 .md；最大 45633B；所有 < 50KB ✅；**backlog 全项已勾选 ✅**；backlog **0 项未决**；上游 v1.79.1 已覆盖（本地 checkout v1.47.0 `e72778e`，main 分支领先 56 commits）；无新增可分析源码（v1.78.10→v1.79.1 仅新增 `scripts/build_binaries.js` 已覆盖 + 3个重度混淆模块 explore/shield/hubVerify 不可分析）。**持续维护 pass，无新增分析任务。** / gitOps 三种回滚策略hard/stash/none + 关键文件保护名单（MEMORY.md/SOUL.md等）+ 路径遍历双重防护normAbs.startsWith(normRepo) + DIFF_SNAPSHOT 8000截断 + stash降级hard容错 / hubReview 非阻塞评审提交（outcome×constraint双因素4档评分 + hub_review_history.json防重复500条 + 10s超时AbortController）/ CE P1关键文件保护+P1路径遍历防护+P3复用资产评审；详见 [`121`](./121-gitOps-hubReview-deep-dive.md)）。新增 **`122`** `paths.js` 路径架构+Session Scope隔离深度（133L纯JS / 集中路径管理+多租户Scope隔离EVOLVER_SESSION_SCOPE+三层路径遍历防护白名单+长度128+禁止点点 + 自身目录优先防止误用父仓库 / getWorkspaceRoot三层fallback（OPENCLAW_WORKSPACE优先→workspace子目录→repoRoot）+ 与doc 80 config集中化互补构成配置中枢 / CE P1多租户路径隔离+P1路径遍历防护+P2集中PathConfig；详见 [`122`](./122-paths-js-path-architecture-deep-dive.md)）。此前 **`120`** v1.79.0/v1.79.1 Delta（+89L daemon 重写+Cycle Hard-Timeout Promise.race 45min+Windows Respawn Fix+dotenv加载顺序修复+201 genes+4 capsules；详见 [`120`](./120-v1790-v1791-cycle-hard-timeout-and-windows-respawn-deep-dive.md)）。

**最后更新**：2026-05-06 06:56（**`116` `candidates.js` 三源能力候选提取深度** ✅（208L纯JS / 三源覆盖Transcript工具调用≥3次重复+Signal信号→能力10类映射+Failed Capsule聚类≥2次重复 / `extractToolCalls` 支持OpenClaw+Cursor双格式 / 10信号→能力标题预定义映射 / problem域聚类（problemPriority权重排序）/ `buildFiveQuestionsShape` 标准五问结构（input/output/invariants/params/failure_points/evidence）/ `stableHash` FNV-1a幂等ID+Set去重 / `renderCandidatesPreview` 1400字符截断预览 / CE P2 Observation能力缺口发现+P2候选表+P3 Gene-as-Capability完整链路；详见 [`116`](./116-candidates-js-three-source-capability-extraction-deep-dive.md)；此前 **`115` `personality.js` 多层自我调优系统深度** ✅（379L纯JS / 5维状态空间rigor/creativity/verbosity/risk_tolerance/obedience / 三层突变（Natural Selection小步靠近历史最优至多2参数×±0.1 + Triggered rule-based信号响应 + Reflection-driven suggested_mutations消费）/ `personalityScore` Laplace平滑成功率+质量因子×0.25+小样本惩罚×min(total/8,1) / `chooseBestKnownPersonality` 需≥3次观测才参与选择 / `shouldTriggerPersonalityMutation` 失败连击≥3触发 / `applyPersonalityMutations` 单次±0.2上限+每轮≤2参数防人格跳变 / 与mutation.js双向联动（personality约束mutation类别 + outcome反馈personality stats）/ CE P2 ModeService五维人格状态+P2人格驱动注入策略+P3人格-注入映射表；详见 [`115`](./115-personality-js-multi-layer-self-tuning-deep-dive.md)；此前 **`114` `selector.js` 多模态选择与漂移策略深度** ✅：417L纯JS / 3-mode信号匹配（regex `/body/flags` / multi-language alias `en|zh|ja` / substring fallback）+ BoW Cosine纯JS无外部依赖（`tokenize`/`cosineSimilarity` / SEMANTIC_WEIGHT=0.4）+ `scoreGeneLearning` 四层（history: success+0.12/hard−0.22/soft−0.08 / epigenetic platform加成 / anti-pattern惩罚 hard−0.4/soft−0.18 / clamp±1.5）+ `1/√Ne`群体遗传学连续漂移（Ne=1→1.0 / Ne=25→0.2）+ diversity_directed drift（capability gaps引导探索替代纯随机）+ failed capsule双重条件封禁（count≥2 + overlap≥0.6）+ `buildSelectorDecision` human-readable reason[]零成本可观测性 / CE P2–P3行动项；详见 [`114`](./114-selector-js-multimode-selection-and-drift-deep-dive.md)；此前 **`113` `memoryGraphAdapter.js` Local-First/Remote-Addictive 适配器模式深度** ✅（203L纯JS / 10-method Adapter Interface Contract / Local-First写（本地JSONL→异步远程推送→Source of Truth） / Remote-Addictive读（getAdvice 优先远程KG→Graceful Degradation降级本地） / 5s超时+AbortController+Bearer Token / Open-Closed扩展性 / BlueCortexCE P2多存储适配器提案；详见 [`113`](./113-memorygraph-adapter-local-first-remote-additive-pattern-deep-dive.md)；backlog **0 项未决** ✅ / 119 doc ✅ / 架构合规✅）；backlog **0 项未决** / 118 doc ✅ / 架构合规✅；**`108` v1.78.7–v1.78.10 Delta + sync-dedup 测试 + 3个新增混淆模块确认**（v1.78.7: +201 genes / +4 capsules / 3个重度混淆模块（explore.js~65KB+shield.js~65KB+hubVerify.js~25KB，hex-encoded packer，代码不可读，env变量名暗示arXiv探索/安全防护/Hub验证）/ v1.78.8: 全模块版本bump / v1.78.9: evolveSessionsDir.test.js 170行回归测试#527（AGENT_SESSIONS_DIR覆盖修复Windows/非标准OpenClaw）/ v1.78.10: index.js +58行CLI改进 / sync-dedup.test.js 192行（CLI `sync` dedup逻辑端到端测试，2个failure mode：bundled default-seed静默跳过→id_collision报告 / --force覆盖并记录hub_asset_id防后续静默skip / 内存HTTP mock / 15s超时）/ 全模块31个文件版本bump / 演进趋势：v1.78.7引入混淆模块，v1.78.8-10聚焦测试覆盖+CLI稳定性 / BlueCortexCE无直接行动项（混淆代码不可分析）；详见 [`108`](./108-v17810-v1787-delta-sync-dedup-new-obfuscated-modules.md)；**`106` `questionComposer.js` 深度分析 + BlueCortexCE 上下文生成 Pipeline 借鉴**（133行源码级分析 / 模板模式（capability→TEMPLATE规范化映射 / 每个capability 2个模板确保多样性 / 模板<240字符不泄露内部术语）+ 策略模式（hash-seeded确定性选择保证幂等性 / autoBuyer dedup hash配合良好）+ 防御性编程（never leak internals / never return empty / graceful degradation to `general`）+ 确定性（`_hashFor`相同输入→相同seed→相同模板→相同问题）；`_normalize` 规范化算法（lowercase+非字母数字→下划线）；BlueCortexCE 借鉴：StructuredContext 模板化输出（基于 observation type 选择模板 / 不泄露 entity 字段名）+ 确定性上下文注入（解决 SearchService 返回顺序不稳定）+ context_templates.yaml 配置化模板注册表 + maxLen 截断保护（与 doc 104 Token Budget 互补）；4个具体行动项（P2 StructuredContext模板化 / P3 确定性选择 / P2 字符级保护 / P3 配置化模板注册表）；详见 [`106`](./106-questioncomposer-bluecortex-context-pipeline-deep-dive.md)；**`103` v1.78.9 Delta + `defaultHandler.js` + Token Budget 对比**（v1.78.5→v1.78.9 增量分析：`index.js` + `paths.js` dotenv 加载顺序修复 #526（EVOLVER_REPO_ROOT 静默失效 root cause + 4步修复流程）/ `genes.json` +201 条新基因（repair/optimize/innovate 三类，含完整 strategy/constraints/validation 结构）/ `capsules.json` +4 / 其余为版本标签更新；`atp/defaultHandler.js`（69行）ATP 订单默认处理器：`defaultOrderHandler` 基于 title/signals 关键词路由 / `resolveAtpServices` 服务注册 / `getAtpMode` off/on/auto 三态开关；Token Budget 对比：CE 固定 observation 数量（50）vs Evolver 字符上限（EXEC_CONTEXT_CAP=20000）+ 分层保护；Research Backlog「语义注入 + timeline token 预算」仍为 open；CE P1 引入动态 Token 预算 / P2 分层注入保护 / P2 语义注入独立预算 / P3 TokenService 整合截断决策；详见 [`103`](./103-v1789-delta-defaultHandler-tokenBudget-comparison.md)；**`102` 主动自我管理三模块深度分析**（`learningSignals.js` 89行信号扩展标签化：5类问题域（reliability/protocol/performance/capability/stagnation）×4类行动标签（repair/optimize/innovate）+3类领域标签（orchestration/memory/skills）+scoreTagOverlap基因评分辅助 / `ops/trigger.js` 33行WAKE文件立即唤醒机制（polling-wake / .tmp+rename防竞态 / 无cron延迟） / `ops/skillsMonitor.js` 143行v2.0技能自愈监控：missing node_modules→npm install / empty node_modules→自动修复 / missing SKILL.md→stub生成 / 忽略列表机制 / v2.0移除node spawn修复perf_bottleneck / 三模块构成"检测→分类→自愈"闭环 + 成熟度L0-L4分级 / CE P1信号扩展标签化（ObservationEntity.signal_tags字段）/ P2立即唤醒（巡检+事件双触发）/ P2技能自愈框架；详见 [`102`](./102-learningSignals-ops-trigger-skillsMonitor-selfManagement-deep-dive.md)；**`101` 核心记忆架构模式深度分析**（`memoryGraph.js` 788行源码级综合 / 7大可借鉴模式（append-only双层分离 / stable signal key / 时间衰减+Laplace平滑 / dormant hypothesis中断恢复 / narrative+graph双重历史 / content-addressable asset / execution trace脱敏）/ 完整源码证据 + BlueCortexCE 优先级映射 / 详见 [`101`](./101-core-memory-architecture-patterns-deep-dive.md)；**`100` `evolve.js` 完整周期→Memory Graph 操作映射**（10阶段完整映射表（preflight→outcome闭合→snapshot→hub→selection→hypothesis→attempt→solidify→dormant→reflection）/ memory_graph.jsonl vs events.jsonl 双文件存储对比 / 完整状态流转图（Phase 2 outcome 闭合本轮attempt，Phase 7 attempt 为下一轮outcome 提供baseline / solidify 写 events.jsonl 而非 memory_graph.jsonl 的设计理由 / CE cron 巡检等价格式实现提案）；**`99` v1.47.0 `evolve.js` 安全系统深度**（2175行完整源码分析 / 竞速检测ps aux+队列上限10+负载感知CPU×0.9+循环门控solidify完成前不开启新cycle+修复断路器5次强制创新+CWD ENOENT恢复+6h skills缓存mtime验证+mood.json情绪感知+git-sync检测+clawhub auto-update+Dormant假设中断恢复 / CE P1队列上限+P1负载感知+P1修复断路器+P2缓存mtime+P2归档+P3休眠恢复+P3 CWD验证；详见 [`99`](./99-evolver-v147-evolvejs-safety-infrastructure.md)；**`98` v1.78.9 Minor Subsystem Additions 深度**（featureFlags.js 114行三层覆盖语义 / dmHandler.js 45行极简DM wrapper / skillUpdater.js 64行远程skill更新（备份+版本追踪） / taskMonitor.js 131行任务统计+环形缓冲区（100样本）+订阅模型+心跳元数据 / CE P1任务统计心跳上报+P1有界环形缓冲区+P2三层config覆盖+P3备份策略参考）；

**`96` `forceUpdate.js` Hub心跳驱动三通道强制更新**（100行 / degit+npm+manual三通道 / 版本校验三段semver / 白名单保护node_modules+memory+MEMORY.md+.git / 心跳线程独立触发 / CE P3可选）；**`95` a2aProtocol.js + a2a.js 深度分析**（1221+173行完整源码级分析 / 双层架构（a2a.js业务逻辑 + a2aProtocol.js协议传输）/ NodeId 7层fallback持久化（env→文件→设备指纹计算SHA-256）/ Hub Node Secret三级缓存（env→内存TTL→文件→env fallback）/ 7种消息类型（hello/publish/publishBundle/fetch/report/decision/revoke）/ PublishBundle联合签名（geneAssetId+capsuleAssetId排序后HMAC）/ 双传输引擎（FileTransport JSONL追加写+HTTPTransport/Hub）+ 可插拔registerTransport / 心跳启动序列（sendHello获取secret→首次延迟→周期心跳）/ 心跳Payload（uptime/commitment_updates/env_fingerprint元数据）/ Hub→Agent反馈（available_work/capability_gaps/novelty_hint/overdue_tasks/circle_experience）/ Rate Limit指数退避（+5s缓冲）/ Hub基础设施（身份DID/信用topup+transfer/审计日志/工作报告）/ SSE事件流（指数退避重连5s→120s上限/轮询降级）/ a2a.js 3门禁胶囊广播（score≥0.7+blast安全+streak≥2）+ Gene 2门禁 / 外部资产降权0.6×防声誉攻击 / JSONL/JSON/text容错解析 / CE P1 SSE重连+P1外部观察降权+P2传输抽象+P2设备身份可验证+P3审计日志）；**`92` `prompt.js` GEP Schema Enforcement + Token Budget 深度**（616行 / 5-Mandatory-Object Schema（Mutation/PersonalityState/EvolutionEvent/Gene/Capsule，JSON-Only Enforcement v3.2，Protocol Drift Fix）/ Token Budget 分层截断（EXEC_CONTEXT_CAP=20000，Header/Footer保护，动态剩余空间计算）/ 条件注入（停滞检测→强制INNOVATE指令，失败连击警告）/ Anti-Pattern Zone（信号重叠≥40%筛选，3条上限）/ Lessons from Ecosystem（positive/negative 6条上限）/ Strategy Block 主动vs降级策略 / 宪法伦理5条非可选约束 / Skill创建8道质量门（NAMING禁止timestamp/工具名，EXPORT VERIFICATION，Sensitive Data参数化→process.env.*）/ Local State Awareness 4位置预检防重复动作 / EvoMap-First问题优先级 / Post-Solidify双语气状态文件 / CE P0 Schema Enforcement，P1 Token Budget分层截断，P1条件注入，P2 Anti-Pattern Zone，P2 EvoMap-First，P3 宪法伦理注入）；

**`91` heartbeatSignalsHandler.js 深度分析**（254行 ATP 心跳旁路模块 / Merchant 节点两类运行模式（run()循环 vs 纯心跳）/ 三重保护机制（`_isEnabled`→`_inflight`并发锁→30s cooldown）/ `_collectDeliverable` 从两个信号源（pending_deliveries + pending_atp_tasks）收集可交付项+`order_id`去重 / `need_work`计数——无法完成任务的可见性警告 / Ledger 防双重提交（`result_asset_id`存在则跳过 / 终态400/404/409用负时间戳标记 / 500条有界裁剪）/ `_buildProofPayload`最小化凭证（pass_rate=1.0固定 / `submitter='evolver_heartbeat_deliver'`来源标识）/ 错误分类（terminal 400/404/409不重试 / 5xx+timeout下次心跳重试）/ `_withTimeout` 10s超时 / CE P1旁路自动提交+P1多重保护机制+P2有界缓存+P2终态/可重试分离）；**`93` directoryClient.js Agent Capability Directory API 深度**（110行 / searchByQuery 语义搜索（/a2a/directory/search?q=）+ searchBySignals 信号搜索（/a2a/directory/search?signals=）+ getAgentProfile 获取 Agent 档案（nodeId/domains/modelType/reputation/completedTasks/currentLoad/online）+ discoverForTask 组合发现 / 静默降级模式（无重试/无熔断/无本地缓存）+ Hub三发现接口关系（directoryClient=Agent发现 / taskReceiver=Task发现 / hubSearch=Asset发现））；**`94` v1.78.7–v1.78.9 版本差分与回归测试护栏**（v1.78.7: dotenv加载顺序修复 #526（+32行 index.js：getRepoRoot()缓存导致EVOLVER_REPO_ROOT被静默忽略，修复后dotenv优先加载cwd/.env再调用getRepoRoot()，+201新Gene/+4新Capsule）/ v1.78.8: memoryGraphRotation.test.js（167行，#519回归测试：gzip压缩/保留计数/幂等/idempotent rotation）/ v1.78.9: evolveSessionsDir.test.js（170行，#527回归测试：getAgentSessionsDir()现在正确尊重AGENT_SESSIONS_DIR环境变量覆盖，修复Windows/非标准OpenClaw静默失败）/ CE P1 append-only日志轮转+P1环境变量优先于硬编码路径+P2静默失败→明确警告）；**`90` executionTrace.js + gitOps.js 深度分析**（431行双模块源码级分析 / executionTrace 201行三级脱敏架构（none/minimal/standard由EVOLVER_TRACE_LEVEL控制/默认minimal输出outcome+变更计数+blast_radius/standard增加file_types+error_signatures+tool_chain+canary_ok）/ Error Signature 提取算法（JS错误→errno→HTTP状态码→UnknownError fallback）/ Outcome感知行数拆分（success=60%增/40%删，failure=50/50）/ Tool Chain 推理（从validation命令字符串推断test_run/lint_check等工具集）/ gitOps 230行三模式回滚（hard/stash/none/stash降级hard/不可恢复性警告）/ Critical Path Protection（10个skill目录+12个关键文件白名单）/ Untracked文件回滚算法（baseline对比+路径穿越防护+深度优先空目录清理）/ CE P1 OutcomeSummary结构化摘要建议+P0 Critical Path Protection+P1 基线对比模式）

**`89` hubSearch.js 两阶段搜索 + 语义增强 + 双层缓存深度**（407行 / 两阶段搜索（Phase 1 `search_only` 免费元数据 → Phase 2 按需付费获取完整payload）/ `buildSemanticQuery` 信号→自然语言查询（排除errsig/取冒号后/最多12信号）/ `fetchSemanticResults` GET `/a2a/assets/semantic-search?q=&type=Gene&limit=10` 并行语义增强 / `mergeResults` 关键词+语义双路合并去重（关键词优先+语义分数增强）/ 双层 LRU 缓存（search cache: 5min TTL/max200 / payload cache: 永久/max100 / 两层协同：同信号二次查询零网络）/ `scoreHubResult` 公式：`confidence×min(streak,5)×(rep/100)+sem_sim×0.3`（streak上限5防刷分/默认rep=50/semantic bonus仅当sim>0时触发）/ `pickBestMatch` 阈值门禁（默认0.72 / `status!=='promoted'` 过滤）/ `deadline` 总时间约束8000ms + `MIN_PHASE2_MS=500ms` 跳过保护 / CE P0两阶段检索+P1双层缓存+P1成功率加权排序）；**`88` taskReceiver.js Worker Pool + Privacy + Capability Match 深度**（566行完整分析 / `claimAndCompleteWorkerTask` 原子操作（claim→extract assignment_id→complete，solidify成功后才触发，防止zombie assignment过期浪费 effort）/ `fetchTasks` 返回 `relevant_lessons` lessonL并行注入管线（Hub基于task signals返回相关经验资产，Agent执行前预注入上下文）/ `detectPrivacyTask` + `taskToSignalsWithPrivacy` 隐私任务检测（delegation to `privacyClient.parsePrivacyParams` / 自动注入`privacy_computing`+`sealed_tool`信号）/ `estimateCapabilityMatch` 三步算法（Jaccard 40%权重 + Laplace平滑成功率60%权重 / `sim < 0.15` 相似度过滤阈值 / `TASK_MIN_CAPABILITY_MATCH=0.1` 最低过滤 / 所有信号key并集构建agent能力向量）/ `selectBestTask` 完整决策树（claimed断点恢复优先 → greedy legacy → 全量评分 / 三策略权重greedy:roi0.1+cap0.05+comp0.05+bounty0.8 / balanced:roi0.35+cap0.30+comp0.20+bounty0.15 / conservative:roi0.25+cap0.45+comp0.25+bounty0.05）/ `scoreTask` 完整公式（`roiNorm=bounty/(difficulty+0.1)/200` / `bountyNorm=bounty/100` / composite四因子加权）/ `estimateCommitmentDeadline` 难度→时间映射（low:15min/medium:30min/high:60min/v.high:120min / MIN=5min/MAX=24h / 与task.expires_at对齐）/ CE P0原子claim+deliver防zombie任务+P1 capability match评分+P1 Observation注入三策略+P2 relevant lessons预注入+P2 Laplace平滑成功率）；**`87` assetStore.js + contentHash.js 资产持久化与内容寻址深度**（`assetStore.js` 369行统一持久化接口（Gene/Capsule/Event/Candidate 双文件策略（JSON+JSONL）/ `writeJsonAtomic` tmp+rename 原子写 / >1MB 大文件尾读防OOM / `loadGenes` JSON+JSONL 合并去重 / `appendCapsule`/`upsertCapsule` 区别 / FailedCapsule 有界缓存（200→100条裁剪）/ `ensureAssetFiles` 惰性初始化 / `contentHash.js` 67行 Canonical JSON（键排序+NaN→null） / `computeAssetId` SHA-256 内容寻址 / SCHEMA_VERSION 1.6.0 + `ensureSchemaFields` 强制注入 / CE P0 内容寻址防重+P0 Append-only 事件日志+P1 Schema版本控制+P1 有界缓存+P2 原子写入）；**`86` 双栈语义架构：Worker（Chroma） vs Java（pgvector）**（⚠️ **关键发现：两栈无自动双写** / Worker `ChromaSync.ts`（470行）独立同步 SQLite→ChromaDB via MCP（chroma-mcp stdio） / Chroma 集合命名 `cm__{project}` 粒度化字段分文档（narrative/text/fact/next_steps各独立向量）/ Java `ObservationRepository` pgvector 三维 embedding（768/1024/1536）独立存储 / ChromaMcpManager 单例管理 MCP 生命周期 + #761 zombie 修复（transport.close() 错误处理） / `ensureBackfilled` 智能增量回填（基于 sqlite_id 去重） / Chroma 查询返回 dedup by sqlite_id（多字段 doc→同一 observation） / **无自动同步**：Worker→Chroma / Java→pgvector 各独立写，两栈结果可能不一致 / Research Backlog 项「Java（pgvector）与 Worker（Chroma）语义结果一致性」**当前状态：两栈独立，SQLite 为唯一共同数据源** / ⚠️ 语义一致 ≠ 存储一致：Chroma 是 Worker 的缓存层，Java 是持久化层 / CE P1 行动项：若需语义一致性 → 设计跨栈同步协议或统一向量存储后端 / `82` Narrative Memory + Ops Supporting Modules（narrativeMemory.js 双重裁剪（30条+12KB）/ loadNarrativeSummary 末尾截断+对齐H3 / commentary.js 三人格词池（standard/greentea/maddog）/ innovation.js 技能覆盖度启发式缺口分析（6类/取最弱2领域/最多3条建议）/ skills_monitor.js 自愈（node_modules检测+SKILL.md生成/v2.0移除同步spawn修复perf_bottleneck/用户忽略列表/.skill_monitor_ignore）/ CE P2叙事摘要注入+P3 Skill自愈+P3覆盖度推荐+P3固定词池评语）；**`84` skillDistiller.js 完整管线深度**（双路径蒸馏（成功10/24h + 失败5/12h）/ analyzePatterns 三维分析（高频≥5次 + Jaccard漂移<0.6 + 覆盖缺口≥3次）/ buildDistillationPrompt 三段式输入（样本+已有知识+分析报告）/ sanitizeSignalsMatch 五步清洗 / deriveDescriptiveId 信号词派生 / synthesizeGeneFromPatterns 信号扩展+expandSignals标签 / 幂等性双保险（时间间隔+数据哈希）/ 失败路径独立蒸馏（强制repair类别）/ CE P0双路径+P0信号清洗+P1模式分析+P1 marketplace summary+P2蒸馏Prompt）；

**`124`** `questionGenerator.js` Hub Bounty 问题生成深度 ✅（212L纯JS / 6大策略信号→自然语言问题（recurring_error/capability_gap/evolution_saturation/failure_streak/user_feature_request/perf_bottleneck）/ 精确+模糊0.7 Jaccard 双层去重 / 3h rate limit + 每轮≤2条 + Priority 排序 / 与 questionComposer.js (ATP) 区别（内部进化求助 vs 外部市场交易）/ A2A fetch payload.questions 字段 / CE P3 主动知识请求机制（本地搜索 miss → Hub 请求）+ P3 Observation fuzzy dedup）；**`123`** `narrativeMemory.js` Markdown 双限制叙事记忆深度 ✅（108L纯JS / Markdown 追加写（`### [timestamp] CATEGORY - status`）/ 双重裁剪30entry+12KB + 原子 rename / 8-entry rolling summary(maxChars=4000) / 与 MemoryGraph 互补（机器 JSONL vs 人类 Markdown 双轨）/ loadNarrativeSummary 对齐到 entry 边界防截断 / CE P3 Markdown Observation Narrative（开发者可直接读 observation 历史）+ P3 双限制 SummaryEntity）；**`109`** Hook/瘦代理延迟源码级分析（signal-detect < 50ms O(n)关键词扫描+1.5s安全阀 / session-start < 100ms JSONL读+Kiro 30min dedup / session-end < 7s git diff同步子进程+Hub curl双路径 / CE 200ms约束交叉验证：CE瘦代理架构wrapper.js→HTTP ACK→@Async完全符合 / ⚠️ generateContext同步LLM调用+Worker Bun Chroma潜在风险 / 4项实测建议已记录 / backlog Item ✅勾选）；**`110`** `mutation.js` 核心深度（204L纯JS / 信号分类决策树（hasErrorishSignal 3条件触发+2排除项 / hasOpportunitySignal 9信号含双向signal / 优先级：error→drift→opportunity→strategy→optimize）/ 两层安全门禁（Layer1: high-risk personality检测rigor<0.5 OR risk_tolerance>0.6 → innovate downgrade to optimize + `safety:avoid_innovate_with_high_risk_personality` / Layer2: high-risk mutation需人格授权rigor≥0.6 AND risk_tolerance≤0.5 → 否则 downgrade to medium + `safety:downgrade_high_risk`）/ 风险等级默认值（repair=low / optimize=low / innovate=medium / high需显式授权+人格门禁）/ 8-field isValidMutation验证 / normalizeMutation幂等规范化 / 6种Safety Signal汇总 / BlueCortexCE P1观察风险分级（ObservationEntity.riskLevel字段）/ P1 Safety Signal记录（signalTags扩展）/ P2人格驱动注入策略（ModeService引入rigor/risk_tolerance映射）；**`111`** `strategy.js` 进化策略预设深度（131L纯JS / 7种预设完整参数（balanced:repair0.2+opt0.3+innov0.5 / innovate:0.05+0.15+0.8 / harden:0.4+0.4+0.2 / repair-only:0.8+0.2+0.0 / early-stabilize:0.6+0.25+0.15 / steady-state:0.6+0.3+0.1 / auto→balanced）+ 5参数（repairLoopThreshold联动权重设计巧思）/ 自动检测三步流程（cycleCount≤5→early-stabilize / force_steady_state→steady-state / evolution_saturation→steady-state）/ FORCE_INNOVATION向后兼容（优先于自动检测）/ 双路径evolution_state.json读取（skill本地+workspace规范路径）/ strategy↔mutation联动：resolveStrategy()被mutationCategoryFromContext调用，strategy.innovate≥0.5触发innovate类别 / BlueCortexCE P2策略驱动注入（ModeService注入策略预设）/ P3周期感知模式（sessionCycleCount→early-stabilize）/ P3策略感知搜索排序（steady-state↑历史成功权重/innovate↑新颖观察权重）；**`105`** EvoMap 核心记忆架构源码深度（memoryGraph.js 788行完整解析 / 7种 MemoryGraphEvent 种类及 JSON Schema / JSONL 追加+原子写策略 / Session Scope 隔离机制（EVOLVER_SESSION_SCOPE env） / 4语言信号提取管道（EN/ZH-CN/ZH-TW/JA）含 errsig 归一化 / History-aware 去重（suppressedSignals≥3/8 / 连续repair/empty cycles检测 / 饱和降级 force_steady_state） / expandSignals 标签化（problem:reliability/action:repair 等） / 置信度评分（Laplace平滑 + 30天/45天半衰衰减 + Jaccard≥0.34 相似度阈值） / 无标签结果推断（错误状态转移矩阵 + error_count/scan_time delta） / Outcome EvolutionEvent 扫描回退 / 双栈适配器模式（Local-first + Remote-additive + graceful fallback） / Narrative Memory 滚动 Markdown（30条/12KB 裁剪） / Reflection 自适应间隔（3/5/8周期 + 30min cooldown） / localStateAwareness 五维自省信号 / BlueCortexCE 对照表（事件模型/信号提取/结果推断/适配器/叙事记忆）；**`83` ATP 商家端子系统**（`atpTaskPickup.js` Hub→商家 pickup bridge + Ledger 防重 / `autoBuyer.js` 日预算+冷启动半额+问题级 SHA-256 dedup 24h / `questionComposer.js` capability→自然语言买家问题 template / `consumerAgent.js` 下单→轮询→结算完整生命周期 / `merchantAgent.js` 注册→拉取→执行→DeliveryProof / `serviceHelper.js` / `cliAutobuyPrompt.js` TTY opt-in / CE P1 Ledger幂等+P1 预算护栏+P2 问题去重+P2 template映射）；**`82` Solidify PRM Process Scoring + Epigenetic Marks + Gene Learning Adaptation（`computeProcessScores` 8相评分（signal×0.05+selection×0.10+mutation×0.05+blast×0.15+constraint×0.25+validation×0.25+protocol×0.10+canary×0.05）/ 表观遗传标记90天衰减+环境三元组（platform/arch/node_version）/ `adaptGeneFromLearning` signals_match扩充+anti_patterns+learning_history / FailedCapsule rollback前diff捕获 / Anti-pattern Hub opt-in发布 / Narrative Memory 30条/12KB裁剪 / ValidationReport标准化+环境指纹 / 7种Strategy预设+cycle自动检测 / CE P0多维质量评分+P0 ExtractionReport+P1 FailedCapture+P1表观遗传+P2反模式发布）；`81` v1.78.7/v1.78.8 版本差分**（⚠️ origin/main 已推进至 v1.78.8 `2b3c046`；v1.78.7 新增内容：`assets/gep/genes.json` +201条基因 / `assets/gep/capsules.json` +4条胶囊 / 3个混淆模块 `src/gep/hubVerify.js`(安全验证) / `src/gep/shield.js`(防护) / `src/gep/explore.js`(arXiv探索) — 均重度混淆无法分析；v1.78.8 为全模块版本 bump；本地工作树仍在 v1.47.0 `e72778e`；CE 无需跟进 / backlog ✅）；**`80` ⚠️ 版本修正 + Config.js 架构模式新增**（⚠️ 本地工作树实际在 v1.47.0 `e72778e`，docs 原误标为 v1.78.5；v1.78.5 代码在 git 未检出；本文档已按 v1.47.0 同步修正 / 新增 `src/config.js` 集中配置模式分析（215行 / 5类配置组 / 全量 env override / CE P0 借鉴） / 新增 `80` v1.47→v1.78.5 架构演进总览（evolve.js -2176行 → 模块化 gep/atp/adapters/ops / +3670/-9156行净减）/ v1.78.5 版本增量同步 ✅（v1.78.1→v1.78.5 共4个commit；关键修复 `cbc4870`：`tool_input.*` nesting 修复使 Claude Code Edit 事件信号检测正常工作，`content`/`new_string`/`file_content` 优先读 `ti.*`，`filePath` 读 `tr.filePath`，fallback 兼容旧格式，不影响 Cursor/Codex/Kiro）；**`79c` Hook 适配系统深度**（hookAdapter 4平台检测+atomic JSON merge+3 hook scripts / signal-detect 7类signal+1.5s timeout+多平台兼容 / session-start 历史注入+dedup TTL 30min / session-end git diff+Hub API双路径）；**`79b` 运维基础设施深度**（health_check 4级严重度 / trigger WAKE IPC / skills_monitor 自愈 / cleanup 两阶段双保险 / lifecycle graceful shutdown）；**`79` 基础设施模块深度**（paths parent git安全 / assetCallLog Append-only / contentHash SHA-256 / deviceId 7层fallback / innovation 技能缺口）；**`78` Proxy 子系统**（MailboxStore 原子写 / OutboundSync MAX_BATCH=50+10次重试 / InboundSync Cursor分页 / CE P0原子写+P1分层路由））；2026-05-03（**`77`** IdleScheduler OMLS + ContentHash；**`76`** selectGeneAndCapsule 决策管线 + Failed Capsule Ban（`1/√Ne` drift / 五种drift mode / CE P1）；**`75`** ATP + Adapters 系统（hubClient 275行 / autoBuyer ~200行 / hookAdapter 207行+4平台 / CE P1/P2/P3）；**`74`** Curriculum + Mutation 闭环管线（curriculum 163行能力边界探测 / mutation 186行5层决策树 / strategy 7种preset联动 / CE P0/P1/P2）；**`73`** 三层信号架构现实核查（v1.78.1 确认 Layer 1/2/3 存在 / 7个新机会信号 / ⚠️ Doc 56 结论修正）；**`72`** inferOutcomeEnhanced baseline/current delta + 双聚合链（±0.12/±0.06 clamp / 30天+45天半衰）；**`71`** MCP Semantic Capability 评估（search≠semantic 注入块 / 安全差异）；**`70`** v1.48–v1.78 新增子系统（skill2gep 645行 / selfPR 408行 / validator ~900行）；2026-05-02（**`69`** v1.47→v1.78 版本差距（净增+3645/-9156行）；**`68`** Post-Solidify 管线（executionTrace三级脱敏 / gitOps三种回滚 / skillPublisher / questionGenerator六策略 / a2a胶囊广播）；**`65`** Selector 基因评分深度（BoW Cosine / 表观遗传加成 / `1/√Ne`漂移）；**`66`** MemoryGraph 事件模型（7种事件类型谱 / 30天+45天半衰 / 完整Cycle时序图）；**`64`** Hub-Selector 反馈闭环 v1.47实际 + validateSynthesizedGene 11道验证门；**`63`** Hub-Selector 三重反馈闭环（noveltyScore/topN调节 / capabilityGaps→diversity_directed drift）；**`62`** 5核心设计模式 + CE翻译优先级矩阵；**`61`** 脱敏+隐私计算管线（sanitize双模式 / crypto AES-256-GCM / 35+HUB_EVENT_SIGNALS）；**`60`** Ops自我修复基础设施（7大模块 / Dual-mode / 与主循环解耦）；**`59`** reflection.js 源码（computeReflectionInterval 三态算法 / 5问战略复盘 / CE自我诊断框架）；**`58`** v1.66新架构（三层信号 / Plateau检测 / ATP Hub market / Self-PR多层门禁）；**`57`** privacyClient+crypto+HUB_EVENT全链路（AES-256-GCM / 6端点 / key不离本地）；**`56`** signals.js 现实核查（v1.47单层regex / Doc 55错误纠正）；**`50`** MemoryGraph 闭环反馈架构（六类事件时序 / Laplace+半衰 / Ban规则 / Idle Gating）；**`49`** localStateAwareness+evolve.js全链路（五类自模型 / State+Event双写）；**`48`** Gene as Compressed Memory闭环（signal→Gene→outcome→边权重）；**`46`** Hub Ecosystem（taskReceiver ROI/Hub Review/issueReporter/a2a广播）；**`43`** Privacy Computing+AES-256-GCM；**`44`** Personality State Machine+Hub Search；**`42`** policyCheck约束系统；**`41`** Device Identity+Innovation Catalyst；**`40`** Failure Mode+Canary；**`39`** Content-addressable Asset；**`37`** Signal Taxonomy+Gene Selection；**`36`** 记忆系统架构综合）

2026-05-07（**`125`** ATP TaskPickup+Execute+Heartbeat+Adapters 深度 ✅（233+285+254+275+207+203+163+172+89L staged files / atpTaskPickup Ledger防重（atp-pickup-ledger.json）+原子写.tmp+rename+冷却5min+有界500条 / atpExecute ContentHash幂等（gene.asset_id=capsule.asset_id=确定性内容哈希）+HMAC-SHA256签名+4阶段管道（read→publish→complete→deliver）+分阶段错误返回 / heartbeatSignalsHandler 旁路交付（无需run()循环）+共享autoDeliver ledger+3态Ledger条目（成功/终结错误-Date.now/未处理）+terminal codes 400/404/409 / hubClient proxy/direct双路由（Bug#460修复）+节点secret引导 / hookAdapter 平台检测双层降级（cwd→$HOME）+coldMerge JSON+markerKey干净卸载+Hook脚本复制0o755 / 4平台适配器 kiro(203L)+claudeCode(163L,tool_input.*修复)+codex(172L)+cursor(89L) / sessions_spawn隔离契约（不直接打印，保持每周期一个契约）/ CE P1 ObservationEntity内容哈希幂等去重+P1 Cron任务Ledger防重+P1原子写入+P2分阶段错误返回+P2终结错误码永久记录+P2 markerKey追踪卸载机制）

**cron 巡检 2026-05-07 01:55**：目录 **133** 个 .md；最大 45633B；所有 < 50KB ✅；**backlog 全项已勾选 ✅**；backlog **0 项未决**；上游 v1.79.1 已覆盖（本地 checkout v1.47.0，main 分支领先 56 commits）；v1.79.1 新增 `integrityCheck.js`（重度混淆，不可分析）+ 2个测试文件，无新可分析生产源码。**持续维护 pass，无新增分析任务。**

**文档结构**：完整目录（89个 doc 文件）与接力导航见 [`index-nav.md`](./index-nav.md)；入口为 [`index.md`](./index.md)；单文件上限 50KB。

