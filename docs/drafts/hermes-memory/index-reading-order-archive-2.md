# Hermes Agent 记忆系统 — 阅读顺序归档（第二卷）

> **归档**：entries 21–50 从 [`index-reading-order.md`](index-reading-order.md) 移出（2026-05-07 归档，原因：防止逼近 50KB 上限）。  
> **返回索引**：[`index.md`](index.md) · [`index-reading-order.md`](index-reading-order.md)（entries 51–79）  
> **早期条目**：entries 1–20 → [`index-reading-order-archive-1.md`](index-reading-order-archive-1.md)

## Entries 21–50

21. **ContextCompressor 完整算法解析**（四阶段压缩 / Tool Pruning 3-Pass / Token-Budget Tail / Structured Template / Iterative Update / Anti-Thrashing / Tool Pair 整治） → [`60-evolution/24-context-compressor-full-algorithm.md`](60-evolution/24-context-compressor-full-algorithm.md)（2026-04-23 新增；整合 06/07/09/17 散落分析）
22. **Hindsight 本地嵌入 Daemon + PostgreSQL Schema**（hindsight-all 包架构 / HindsightEmbedded vs HindsightServer / Profile 机制 / pgvector/pgvectorscale/vchord 多扩展 / 连接池 / Schema 隔离 / LLM Provider 支持 / Docker 部署对比） → [`60-evolution/25-hindsight-local-embedded-daemon-and-postgresql-schema.md`](60-evolution/25-hindsight-local-embedded-daemon-and-postgresql-schema.md)（2026-04-23 新增；源自 Hindsight 官方安装文档 + API 参考 + Hermes 插件源码）
23. **Supermemory Multi-Container & Search Mode**（多容器架构 / 搜索模式对比） → [`60-evolution/26-supermemory-multi-container-and-search-mode.md`](60-evolution/26-supermemory-multi-container-and-search-mode.md)
24. **ContextEngine 可插拔压缩架构**（ABC / 插件发现 / 生命周期 / Token 追踪 / 与 MemoryProvider 对比） → [`60-evolution/27-context-engine-pluggable-architecture.md`](60-evolution/27-context-engine-pluggable-architecture.md)（2026-04-24 新增；源自 `agent/context_engine.py`）
25. **Prompt Caching 与记忆系统交互分析**（system_and_3 策略 / 压缩-缓存失效恢复 / CE 架构差异 / 可执行借鉴） → [`28`](60-evolution/28-prompt-caching-and-memory-interaction.md)（2026-04-24 新增；源自 `agent/prompt_caching.py` + `run_agent.py` 源码）
26. **MemoryProvider Hooks 高级专题（续）**（BlueCortexCE vs Hermes Summary Template 逐字段对比 / SessionSearch LLM 截断三层 fallback / RetainDB Supermemory 补充） → [`60-evolution/29-memory-provider-hooks-advanced-topics.md`](60-evolution/29-memory-provider-hooks-advanced-topics.md)（2026-04-24 拆分自 `06` §43–§44 + 合并自 `07` §44–§45）
27. **矛盾检测工程方案 + Session Tools 分析**（Entity Extraction / SessionSearch 双模式 / memory_tool 语义 / Tool Result Pre-pass / SessionDB v6 Reasoning Chain / Honcho write_frequency） → [`60-evolution/30-contradiction-detection-and-session-tools.md`](60-evolution/30-contradiction-detection-and-session-tools.md)（2026-04-24 拆分自 `07` §45–§52）
28. **Context References — @-Prefix 文件/URL/Git 展开**（6 类引用 / Token 50%+25% 预算 / 路径安全双层 / `allowed_root` 隔离 / async URL fetcher / `cli.py`/`gateway/run.py` 双集成点 / CE 差距 + 可执行借鉴） → [`31-context-references-file-expansion.md`](60-evolution/31-context-references-file-expansion.md)（2026-04-24 新增；`agent/context_references.py` 520 行完整解析）
29. **上游新提交分析（2026-04-25）**：Session Finalize Hook（`260ae621` / expiry flush 后触发）/ ContextEngine ABC 强化（`has_content_to_compress` / `focus_topic` / `a9a4416c`）/ Hindsight bank_id_template（`edff2fbe`）/ Hindsight Bug Fixes 批量（`f9c6c5ab` document_id per-process 等） → [`32-upstream-new-commits-session-hooks-and-context-engine.md`](60-evolution/32-upstream-new-commits-session-hooks-and-context-engine.md)（2026-04-25 新增）
30. **Context Compressor 安全强化 + Bug Fix #10896（2026-04-25）**：Bug Fix #10896（压缩丢失活跃 user 消息 / `_ensure_last_user_message_in_tail`）/ JSON-aware tool call args 截断（防 400 死循环）/ `redact.py` 全链路 secrets 过滤（JWT/URL params/form bodies/Discord mentions）/ multimodal content 安全操作 / Structured Summary "Active Task" 首位字段 + 语言感知 / `sanitize_context()` 防递归注入强化 / `emit_collect()` decision-style hooks / `hermes_cli/hooks.py` 新增 CLI 管理工具（list/test/revoke/doctor） → [`33-context-compressor-security-hardening-and-bugfixes.md`](60-evolution/33-context-compressor-security-hardening-and-bugfixes.md)（2026-04-25 新增）
31. **上游新提交分析（2026-04-25 ~ 04-26）**：`e69526be` → `origin/main`（~1590 commits）中 13 个内存相关核心提交：ContextEngine ABC `has_content_to_compress()` 修复 plugin 兼容性（`a9a4416c`）/ 多模态 content 安全操作（`1e8254e5`）/ 语言感知摘要（`13294c2d`）/ Summary fallback NameError 修复（`c0385873`）/ Session 生命周期大重构 — 删除 `on_session_reset` hook，简化为 `commit_memory_session` + `rotate_memory_session` 双接口（`7cb06e3b` + `8275fa59` + `7856d304`）/ Shell Hooks — 用户 shell 脚本作为 hook 回调（`3988c3c2`）/ Redact config bridge fix（`0e235947`）/ `emit_collect()` decision protocol（`51ca5759`）/ Honcho 5-tool + 双层 dialectic context injection（`cc6e8941`） → [`34-upstream-new-commits-session-lifecycle-and-context-engine.md`](60-evolution/34-upstream-new-commits-session-lifecycle-and-context-engine.md)（2026-04-25 新增）
32. **Hindsight 批量修复 + Compression Eval Harness 设计**（`cc6e8941` → `6f1eed39`，~1340 commits）：Hindsight 5 连击 — 共享 event loop 关停修复（`93a74f74` #11923）/ 可配置 `HINDSIGHT_TIMEOUT`（`403c82b6`）/ `_run_sync` 中配置 timeout 实际生效（`f1ba2f0c`）/ document_id per-process 修复 session resume 数据丢失（`f9c6c5ab` #6602）/ `bank_id_template` 运行时动态派生（`edff2fbe`）/ Codex OAuth context length 缓存 400k 硬上限主动失效（`346601ca` #15078）/ Compression Eval Harness 设计 PR（`9f5c13f8`，未合并，Phase 3 Structured Extraction 质量保障直接可迁移）/ `pre_gateway_dispatch` gateway hook（`1ef1e4c6`） → [`35-hindsight-batch-fixes-and-compression-eval-harness.md`](60-evolution/35-hindsight-batch-fixes-and-compression-eval-harness.md)（2026-04-25 新增）
33. **上游新提交分析（2026-04-11 ~ 04-24）**：压缩地板 64K + Budget 警告移除 + Activity 追踪（`ce6fb1c2`）/ Secrets redaction 两阶段（`1f804d17` + `fcae077d`）/ Context Engine 插件发现系统（`fec7b222`，4 层选择优先级）/ Compression Eval Harness 设计（`9f5c13f8`，scrubber pipeline + 6 维度 probe）/ Session Artifact Disk Cleanup（`62919b1e`，retention + safety + automated daily cleanup）/ Stuck session resume loop fix（`46db738e` #7536） → [`36-upstream-new-commits-compression-floor-disk-cleanup.md`](60-evolution/36-upstream-new-commits-compression-floor-disk-cleanup.md)（2026-04-25 新增）
34. **Honcho 双维 Cadence 门控机制**（2026-04-25 新增）：`contextCadence` + `dialecticCadence` 双维独立刷新 / `_turn_count` 驱动 + prewarm-as-turn-0 机制 / `on_turn_start` 调用顺序（`run_agent.py` 中必须先于 `prefetch_all`）/ 空结果 streak 回退（最多 8× cadence）/ 双层注入（base context + dialectic supplement，隔离缓存）/ `liveness_snapshot()` 可观测性 API / CE 分层刷新 + empty-streak 回退可执行借鉴 → [`50-honcho-holographic-deep/07-honcho-cadence-gating-mechanism.md`](50-honcho-holographic-deep/07-honcho-cadence-gating-mechanism.md)（2026-04-25 新增；`plugins/memory/honcho/__init__.py` 1253 行完整解析）
35. **上游新提交分析（2026-04-25）**：`6a957a74` Write Origin Metadata（`_build_memory_write_metadata` / signature-inspected backward-compatible metadata 传递 / 三处调用点: flush+tool+async dispatch）/ Tool Call Repair Chain 三层（streaming assembly / control char / send chokepoint）/ `fd3864d8` / `8ea389a7` → [`37-upstream-new-commits-write-origin-metadata-and-tool-call-repair.md`](60-evolution/37-upstream-new-commits-write-origin-metadata-and-tool-call-repair.md)（2026-04-25 新增）
36. **上游新提交分析（2026-04-25）**：`00c3d848` Skip External-Provider Sync on Interrupted Turns（`_sync_external_memory_for_turn` 提取 / 三层 skip 条件 / 信任边界修复 `#15218` / 16-case 测试矩阵）/ `1e8254e5` Guard ContextCompressor Against Structured Content（`_content_text_for_contains` / `_append_text_to_content` 多模态安全操作）/ `b66644f0` Richer Session-Scoped Retain Metadata（`retain_tags`/`retain_source` knobs / Gateway Session Identity 穿透到 Provider / chat_id/thread_id/user_name） → [`38-upstream-new-commits-interrupted-sync-structured-content-retain-metadata.md`](60-evolution/38-upstream-new-commits-interrupted-sync-structured-content-retain-metadata.md)（2026-04-25 新增）
37. **Session Auto-Prune + Secrets Redaction + Bug Fixes（2026-04-25）**：`b8663813`（Session Auto-Prune + VACUUM at Startup，`state_meta` 幂等表，`auto_prune` opt-in，`min_interval_hours` 跨进程协调）/ `3368814a`（Secrets Redaction in Context Compaction 三层防御：input redaction + prompt 禁止 + output redaction）/ `c0385873`（Summary Model Fallback NameError Fix）/ `a9a4416c`（ContextEngine ABC `has_content_to_compress()` plugin 兼容性修复 + `focus_topic` 入 ABC 签名） → [`39-session-auto-prune-secrets-redaction-and-bugfixes.md`](60-evolution/39-session-auto-prune-secrets-redaction-and-bugfixes.md)（2026-04-25 新增；CE 安全缺口：extractedData JSONB 无 redaction）
38. **Gateway /resume 压缩链追踪修复（2026-04-25）**：`19a3e2ce`（Bug #15000：压缩后 `/resume <title>` 加载空 session — `SessionDB.resolve_resume_session_id()` 链追踪 parent→child 返回有消息的 descendant；Gateway `_handle_resume_command` 在 `resolve_session_by_title` 后调用；`started_at DESC` 选最新 child，深度 32 上限；CLI 侧同 Bug 在 `f24956ba` 已修复，Gateway 侧遗漏至此） → [`60-evolution/40-gateway-resume-compression-continuation-chain.md`](60-evolution/40-gateway-resume-compression-continuation-chain.md)（2026-04-25 新增；49 新提交中唯一内存相关，其余 48 个非内存）

41. **Built-in MemoryTool & SessionSearchTool — Tool-Calling 接口设计（2026-04-25）**：`tools/memory_tool.py`（584L）MemoryStore 双态快照模型（`_system_prompt_snapshot` vs live entries）+ Threat Scanning 写入边界（injection/exfil 正则 + 不可见 Unicode）+ fcntl 排他锁 + 原子重命名 + 字符预算强制（2200/1375 chars）；`tools/session_search_tool.py`（590L）FTS5+LLM dual-mode search + truncate-around-matches 上下文窗口策略 + 并行 LLM summarization with semaphore concurrency limit → [`41-builtin-memorytool-and-sessionsearchtool.md`](60-evolution/41-builtin-memorytool-and-sessionsearchtool.md)（2026-04-25 新增；独立于 Provider 体系的 Agent 主动调用工具层）

42. **Delegate Tool 内存交互模型（2026-04-25）**：`skip_memory=True` 子代理完全隔离（无 MemoryStore/MemoryManager）+ `on_delegation()` 钩子机制（task+result+baby_session_id 广播给 Provider）+ Provider 全未实现该钩子（grep 无结果）+ `max_concurrent_children > 10` 成本警告 + leaf/orchestrator 双角色 memory 工具全禁 → [`42-delegate-tool-memory-interaction-model.md`](60-evolution/42-delegate-tool-memory-interaction-model.md)（2026-04-25 新增；53 个新上游提交均非内存相关，本轮填补委托-记忆交互空白）

43. **上游新提交分析（2026-04-25）**：on_memory_write 顺序路径桥接修复（`46f7b38b`，#10174 — 外部 Provider 丢失所有单工具调用记忆写入通知）+ Per-User Memory Scoping（`c52e5931` — Gateway 多用户 user_id 透传到 Provider，Mem0/Honcho 支持 per-user 隔离）+ Hindsight 小修复两连（`8877688b` timeout 修复 / `9d42aca2` LLM key 保留） → [`43-on-memory-write-bridge-and-per-user-scoping.md`](60-evolution/43-on-memory-write-bridge-and-per-user-scoping.md)（2026-04-25 新增；4 个内存相关提交，上游共 ~100 个新 commit）

44. **RetainDB Provider 深度解析（2026-04-25）**：`plugins/memory/retaindb/__init__.py`（766 行）完整源码分析 — SQLite write-behind queue（crash-safe、启动重放、thread-local 连接、无限重试）/ Dialectic synthesis（`ask_user` API、推理层级自适应）/ SOUL.md agent seeding / 5 工具共享文件存储（`rdb://` URI）/ memory_type 6 类枚举 / Project 解析策略 / `on_memory_write` 镜像 hook → [`44-retaindb-provider-deep-dive.md`](60-evolution/44-retaindb-provider-deep-dive.md)（2026-04-25 新增；RetainDB 766 行无独立分析文档，本次填补空白）

45. **on_session_finalize Expiry Flush + Hindsight CPU Detection + Redact Config Bridge（2026-04-25）**：`260ae621`（`on_session_finalize` 在 expiry flush 时触发，4 调用点全览 / Provider 均未实现该 hook / 与 /resume 关系缺口）+ `25465fd8`（回归测试）+ `df55660e`（Hindsight `_check_local_runtime()` CPU 检测graceful degrade）+ `0e235947`（`security.redact_secrets` config.yaml → env var 桥接在 `setup_logging` 之前，151 行测试覆盖） → [`45-on-session-finalize-expiry-flush-cpu-detection-and-redact-bridge.md`](60-evolution/45-on-session-finalize-expiry-flush-cpu-detection-and-redact-bridge.md)（2026-04-25 新增；~1645 commits 扫描，4 个新 memory 相关发现）
46. **Supermemory Provider 深度解析（2026-04-25 新增）**：791 行源码完整分析 — 三类 deduplication（static/dynamic/search）/ trivial message 过滤（`_TRIVIAL_RE`）/ profile frequency 节流（50 轮一次）/ write gating（`agent_context` 感知：cron/flush/subagent）/ multi-container 架构（`{identity}` 模板变量）/ entity context 可配置提取提示词 / session-end 批量摄入 / 4 工具设计 → [`46-supermemory-provider-deep-dive.md`](60-evolution/46-supermemory-provider-deep-dive.md)（2026-04-25 新增；填补 doc 09 之外的 Provider 内部架构空白）
47. **Mem0 Provider 深度解析（2026-04-25 新增）**：373 行源码完整分析 — server-side LLM extraction（vs local embedding）/ circuit breaker（5-failure, 120s cooldown）/ per-user + per-agent 双层过滤 / reranking 分级搜索 / `infer=False` 显式存储跳过 extraction / thread-safe 懒初始化 / API response 归一化 → [`47-mem0-provider-deep-dive.md`](60-evolution/47-mem0-provider-deep-dive.md)（2026-04-25 新增；8 Provider 中最后两个无独立分析文档的已完成）
48. **flush_memories 彻底移除 + 背景评审循环接管（2026-04-26 新增）**：`ea01bdce` (#15696) — 删除 ~248 LOC `AIAgent.flush_memories()` + 所有调用点（cli / gateway / compression 前）；四大缺陷（阻塞 / 破坏 Prompt Cache / Gateway 双进程额外破坏缓存 / 冗余）；新架构 `_spawn_background_review` daemon thread（每 10 轮触发一次，不破坏缓存）；`SessionEntry.memory_flushed` → `expiry_finalized`（向后兼容）；`commit_memory_session` 替代 flush（调用 `MemoryProvider.on_session_end()`）；ContextCompressor 模型切换 token 预算重算修复（`5401a008`）；CE Phase 3 对照：结构化提取 vs 通用 AI 评审互补 → [`48-flush-memories-removal-and-background-review-architecture.md`](60-evolution/48-flush-memories-removal-and-background-review-architecture.md)（2026-04-26 新增；e5647d78..origin/main ~56 commits 上游扫描，核心发现）
49. **Compression Eval Harness + Phase 3 质量评估借鉴（2026-04-26 新增）**：`1e6285c5` — 上游新增 probe-based 压缩质量评估框架（`scripts/compression_eval/`）；两阶段（Continuation + Grading）；6 维度 rubric（accuracy/context_awareness/artifact_trail/completeness/continuity/instruction_following）；Fixture scrub pipeline（redact + paraphrase + truncate）；**直接迁移用于 Structured Extraction 质量评估** — Session observations 作为 fixture、extraction output 作为 answer、adapted rubric 评分；CE Phase 3 acceptance test 补充：功能正确性（acceptance test）+ 提取质量维度（eval harness）；Session-scoped reasoning override（`25ba6a4a`）+ context custom provider context_length（`125de020`）补充 → [`49-compression-eval-harness-and-structured-extraction-quality.md`](60-evolution/49-compression-eval-harness-and-structured-extraction-quality.md)（2026-04-26 新增；ea01bdce..cec0af02 ~77 commits 上游扫描，核心发现）

50. **压缩边界信号 · Hindsight 空闲超时 · 压缩机鲁棒性（2026-04-27 新增）**：见 [`50-compression-boundary-hindsight-idle-timeout-and-compressor-bugfixes.md`](60-evolution/50-compression-boundary-hindsight-idle-timeout-and-compressor-bugfixes.md)（cec0af02..origin/main ~50 commits 上游扫描，7 个内存相关发现）

51. **模型切换 Token 预算重算 + Background Review Agent 工具集限制（2026-04-27 新增）**：`e5647d78..origin/main`（374 commits）中 2 个记忆相关发现 — `5401a008`（`update_model()` 三字段同步重算 bug；tail_token_budget/max_summary_tokens 在模型切换时未更新；200K→32K 切换导致 20K vs 10K 预算偏差）+ `8ad29a93`（Background review agent 显式限制 `toolsets=["memory", "skills"]`，防越权操作）；其余 372 个非记忆相关（UI/TUI/平台集成/Backup/Approval）；⚠️ 本地 Hermes Agent Repo 已于 22:03 删除，需重新 clone 才能继续代码实地分析 → [`51-context-compressor-model-switch-and-background-review-toolset.md`](60-evolution/51-context-compressor-model-switch-and-background-review-toolset.md)（2026-04-27 新增；e5647d78..origin/main ~374 commits 上游扫描）

52. **Session Teardown 真实历史传递 · Cross-Provider Reasoning Leak · Hindsight Setup 预填充 · Transcript 文件清理（2026-04-27 新增）**：`cec0af02..origin/main`（267 commits）中 4 个记忆相关发现 — `500774e3`（Gateway `_cleanup_agent_resources` 向 `shutdown_memory_provider` 传递 `agent._session_messages` 而非空列表；Holographic/Hindsight early-return guard 导致 restart/reset/expiry 后首 Turn 报"找不到相關的對話記錄"）+ `a59a98b1`（CLI exit cleanup 同 bug）+ `ee1a07f9`（跨 Provider reasoning leak：MiniMax→DeepSeek 时 foreign `reasoning` 被 promote 为 `reasoning_content`，导致 HTTP 400；DeepSeek 的 `reasoning_content=''` pin 使该 shape 只可能来自 prior provider）+ `64a497bf`（Hindsight setup 重新运行时预填充现有配置；mode/llm_provider/llm_base_url/llm_model）+ `3b60abb6`（`delete_session`/`prune_sessions` 删除 SQLite 记录同时清理 .json/.jsonl transcript 文件；防止 ~27MB/天磁盘增长）；其余 263 个非记忆（UI/TUI/Backup/Approval/Slack/Google Meet）；重新 clone 后上游扫描 → [`52-session-teardown-fix-cross-provider-reasoning-and-filesystem-cleanup.md`](60-evolution/52-session-teardown-fix-cross-provider-reasoning-and-filesystem-cleanup.md)（2026-04-27 新增；cec0af02..origin/main ~267 commits 上游扫描）

53. **Session Switch Hooks + ContextCompressor 多项修复 + Hindsight Single-Writer 重构（2026-05-02 新增）**：`cec0af02..origin/main`（991 commits）中 11 个记忆相关核心发现 → [`53`](60-evolution/53-session-switch-hooks-context-compressor-and-hindsight-refinements.md)：`13683c08`（MAJOR）— `on_session_switch()` 新钩子 / `f0dc919f`（MAJOR）— Token 估算含 system prompt+schemas / `b194617d` — off-by-one tail fix / `dad02174` — Honcho RLock / `0a5ee01e`+`c38dac74`+`0565497d` — Hindsight 重构 / `6ea5699e` — aux model failure notification / `e553f6f3` — scrub surface 收缩 / `142b4bf3` — session_search 按 last_active 排序 / `b29b709a` — `call_id` vs `id` 多模型兼容。
54. **上游新提交分析（2026-05-03 → 05-04）**：`d87fd9f0..origin/main`（35 commits → 5 commits），**0 个记忆相关** — write_file 零字节修复 / cron non-dict origin / approval credential 扩展 / gateway home-channel 保留；结论：记忆系统文档保持最新 → [`54`](60-evolution/54-upstream-new-commits-may-03.md)
55. **Compressor Dedup Pass 非字符串 Content 安全防护（2026-05-04 新增）**：`408dd8aa` — `ContextCompressor` deduplication pass 中 `.encode()` 前未校验 content 类型，非字符串 content 导致 `AttributeError`；修复增加 `if not isinstance(content, str): continue` guard；多模态 tool result 场景防护；CE 借鉴：tool result processor 应对 content 字段保持类型警觉 → [`55-compressor-dedup-non-string-content-fix.md`](60-evolution/55-compressor-dedup-non-string-content-fix.md)（2026-05-04 新增；d87fd9f0..origin/main 35 commits 中唯一记忆相关发现）

56. **双层压缩架构分析（2026-05-04 新增）**：`trajectory_compressor.py` (65KB) + `agent/trajectory.py` + `agent/insights.py` — Session 级 ContextCompressor（实时，保持活跃对话在 context limit 内）与 TrajectoryCompressor（离线，压缩轨迹为 RL 训练数据）的完整对比；压缩策略差异（Token budget tail vs Fixed turns protection）；ShareGPT JSONL 格式轨迹持久化；InsightsEngine 历史会话分析（token/cost/tool 使用模式）；BlueCortexCE 借鉴：双层分离架构、轨迹分段保护策略、ShareGPT 格式标准化、Insights API 端点设计 → [`56`](60-evolution/56-trajectory-compression-vs-session-compression-dual-layer-design.md)（2026-05-04 新增；首次深度覆盖 RL 训练数据 pipeline）

57. **上游新提交分析（2026-05-04 补充）— 无新增记忆相关变更**：`d87fd9f0..a11aed1ac`（~20 commits），**0 个记忆相关** — Docker 单容器化 / CLI launch directory / TUI npm peer-flag / write_file 参数校验 / Cron non-dict origin；结论：文档保持最新，下次巡检从 `a11aed1ac` 起扫描 → [`57-upstream-no-new-memory-commits-through-may-04.md`](60-evolution/57-upstream-no-new-memory-commits-through-may-04.md)（2026-05-04 新增）

58. **惰性 Session 创建与 Ghost Session 防护（2026-05-04 新增）**：`c5b4c481` (#18370) — 移除 `AIAgent.__init__()` 中的 eager `create_session()` 调用，改为 `_ensure_db_session()` 在 `run_conversation()` 首次消息 flush 时按需创建；`_session_db_created` 标志位 + INSERT OR IGNORE 幂等插入；一次性迁移 `prune_empty_ghost_sessions()` 清理 24h 前无消息的 TUI 空 session；CE 借鉴：SessionEntity 惰性创建、幂等插入、ghost session 清理；附 `f1e029251` crash/restart 后 session resume 机制 → [`58`](60-evolution/58-lazy-session-creation-and-ghost-session-prevention.md)（2026-05-04 新增；源自 `run_agent.py` + `hermes_state.py` + `cli.py` 源码）

59. **上游新提交分析（a11aed1ac → origin/main，85 commits，2026-05-04 新增）**：3 个记忆系统相关 + 1 个工具结果存储相关：`6b88f46c5`（Compressor timeout fallback fix：HTTP 408/429/502/504 及 timeout 字符串触发 fallback 到主模型，防止上下文无限增长）/ `e2211b268`（`on_session_reset()` 清理 `_summary_failure_cooldown_until` 防止新 session 被旧 cooldown 阻塞）/ `c653f5dc3`（session_search auxiliary model 文档澄清）/ `e50809b77`（`read_file` max_result_size_chars=100K 封顶，闭合 tool_result_storage.py Layer 2 防御缺口）；CE 借鉴：StructuredExtractionService 增加 transient vs permanent 错误分类、session 重置清理所有 transient 状态、tool result 增加大小封顶；其余 81 个非记忆相关（Dashboard/Kanban/TUI/Provider） → [`59`](60-evolution/59-upstream-a11aed1ac-to-origin-main-memory-analysis.md)（2026-05-04 新增；下次扫描起点：`origin/main` `110387d14`）
60. **上游新提交分析（110387d14 → origin/main，89 commits，2026-05-05 新增）**：3 个记忆系统相关 + 1 个上下文管理相关：`a7417f8a4`（⭐ P2）— Compressor Pass 2（非字符串 content guard，延续 doc 55 的 Pass 1 修复）/ `b7bbc6250`（⭐ P2）— `_prune_old_tool_results` 边界方向修复（budget count-space 转换防静默截断）/ `6b4ccb9b1`（⭐ P2）— session_search 报告来源从 FTS5 child session 修正为已解析父 session/ `d29f90e89` — Error Classifier 大上下文假溢出修复（非直接记忆）；其余 86 个非记忆相关 → [`60`](60-evolution/60-upstream-110387d14-to-origin-main-memory-analysis.md)（2026-05-05 新增；下次扫描起点：`origin/main` `e493b1c48`）

64. **上游新提交分析（a7417f8a4 → 0ce1b9fe2，8 commits，2026-05-05 新增）**：**0 个记忆相关** — TUI prompt separator / Teams plugin slash exec / Opus 4.6 fast mode 限制 / AUTHOR_MAP；结论：文档保持最新 → [`64`](60-evolution/64-upstream-no-memory-commits-through-0ce1b9fe2.md)（2026-05-05 新增；下次扫描起点：`origin/main` `0ce1b9fe2`）

60. **TUI FD Leak + 压缩机 Cooldown Session Reset（2026-05-04）**：`6da970f15`（TUI session teardown FD leak fix：`session.close()` 现调用 `AIAgent.close()` 关闭 httpx transport pool）/ `e2211b268`（`on_session_reset()` 清理 `_summary_failure_cooldown_until`）；CE 借鉴：session teardown 必须清理所有资源（包括 HTTP 客户端）、transient cooldown 状态必须在 session reset 时清理 → [`60-upstream-fd-leak-and-compressor-cooldown-reset.md`](60-evolution/60-upstream-fd-leak-and-compressor-cooldown-reset.md)（2026-05-04 18:03 创建）
