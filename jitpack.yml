# JitPack build config: build only cortex-mem-spring-integration (excludes backend)
# See: https://docs.jitpack.io/building/

jdk:
  - openjdk21

install:
  - cd cortex-mem-spring-integration && mvn clean install -DskipTests
